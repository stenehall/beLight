var assert = require('chai').assert;

describe('Schedule', function(){

  it("should check for schedule in config");
  it("should parse loaded schedules correctly");
  it("should handle incorrect schedules gracefully");
  it("should actually add schedules");

});